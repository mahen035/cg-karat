server:
  port: 8084
spring:
  application:
    name: inventory-service
  datasource:
    url: jdbc:postgresql://localhost:5432/karatdb
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: pass123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
